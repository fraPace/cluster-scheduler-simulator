package ClusterSchedulingSimulation.utils


object Constant {
  @inline final val KiB: Long = Math.pow(2, 10).toLong
  @inline final val MiB: Long = Math.pow(2, 20).toLong
  @inline final val GiB: Long = Math.pow(2, 30).toLong
}
